import 'package:flutter/material.dart';

class TrocasPage extends StatelessWidget {
  const TrocasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('MINHAS TROCAS'),
        automaticallyImplyLeading: false,
      ),
    );
  }
}
